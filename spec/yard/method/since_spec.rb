RSpec.describe Yard::Method::Since do
  it "has a version number" do
    expect(Yard::Method::Since::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
